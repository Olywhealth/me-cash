package com.mecash.enums;

/**
 * The only two currencies that exist in the meCash world.
 *
 * <p>Today's reference exchange rate is {@code A -> B = 1.3455} (see
 * {@code mecash.exchange.rate-a-to-b}). The reverse rate is derived as its inverse.
 */
public enum Currency {
    A,
    B
}
